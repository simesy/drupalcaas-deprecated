name: lecapi
recipe: drupal9
config:
  webroot: web
  xdebug: false
tooling:
  drush:
    # Perhaps temporary fix for drush tooling using the wrong service.
    service: appserver
services:
  searchsolr:
    type: solr:7.7
    portforward: true
    core: collection1
    config:
      dir: .platform/configsets/solr7
